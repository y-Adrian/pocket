### 命名规范

**函数名、变量名**： 统一使用蛇形命名法，即单词用小写字母，多个字母之间用下换线分隔
**文件夹名**：统一使用小写字母，用 - 隔开
**文档名称**：统一使用小写字母，如果有多个单词，用 - 隔开

### 目录规范

### 头文件组织

每个模块的头文件，如果是自己使用，放在同级别源码目录下即可。如果是暴露给外部其他模块使用的头文件，则需要放在单独的 include 目录下。

### C++编码风格
